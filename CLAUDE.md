# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

A **D&D campaign tracker** — a personal tool for the project owner, who is the **Dungeon Master (DM)**, with a **website** his players log into to track their own characters. Built phase-by-phase as both a real tool and a tech-learning project (the natural "next level up" from the sibling `stock-tracker` and `cloud-habit-tracker` projects: this one is the first genuinely **multi-user, authenticated, hosted web app** in the workspace).

The owner and his players are **new to D&D**, so the app doubles as a way to *learn the game*: it surfaces rules text (spell/ability descriptions, dice) inline so the table doesn't need to memorize the rulebook.

**Posture:** mostly **track** (record-keeping), with targeted **assist** features (dice roller, combat math, rules lookup) where they clearly earn their place.

## Roles & audience

- **DM (owner)** = admin. Sees and edits the entire campaign world, including hidden/secret entities the players don't know about.
- **Players (friends)** = each logs in, sees only their own character and the things they've discovered or the DM has shared.

## The two architectural spines

Everything in this app hangs off two core ideas. Get these right and the features are mostly views on top.

### 1. The creature / stat-block engine

A **player character, an NPC, and a monster are the same kind of thing**: a *creature* with a stat block (six ability scores, HP, AC, resistances/immunities, conditions, abilities). Model this **once**. The `kind` column ('pc' | 'npc' | 'monster') is the only thing that distinguishes them — a tavern keeper is just a low-stat NPC. Two world-flavor traits also live on every creature (migration 2):

- **`disposition`** — a 5-point spectrum, hate→love: 'hostile' | 'unfriendly' | 'neutral' | 'friendly' | 'allied'. How the creature treats the party. The DM can flip it **live** from the creature's page (a friendly merchant turning hostile mid-scene), not just via the edit form. Surfaced for NPCs/monsters.
- **`alignment`** ('' | LG..CE) — the creature's moral compass. A separate, more granular axis from disposition; applies to anyone, including PCs.

- A **character sheet** = the full, editable creature view (player-owned).
- The **monster inspector** (Baldur's Gate 3-style "inspect") = a trimmed, read-only creature view.
- **Combat tracking** operates on creatures generically, so it works for PCs and monsters with no special-casing.

### 2. The public/private visibility spine ("fog of war")

Every **entity** (NPC, location, map marker, monster, even individual monster stats and abilities) carries a **visibility state** per viewer:

- The **DM sees everything.**
- **Players see only what they've discovered** (invisible / partially-known / fully-known).

This is **not one feature** — it's a rule the character sheets, monster inspector, map overlays, campaign blog, and entity sidebar all enforce. **Bake a `visibility` concept into the data model from Phase 1**, even while the app is still single-user (DM-only). Real enforcement against player logins arrives in the auth phase, but the schema must anticipate it from the start.

## Shared "entity" concept

The **sidebar list of known things**, the **map markers**, and the **things mentioned in the campaign blog** all point at the *same underlying entities* (NPCs, locations, factions, items). Model entities **once** and surface them in multiple views — do not build three parallel lists.

## Tech decisions

- **Language / framework:** Python + **Flask** (continuity with `stock-tracker`; owner already knows the patterns).
- **Storage:** **SQLite** (stdlib `sqlite3`) to start, same as stock-tracker Phase 3+. Revisit only if hosting demands it.
- **Frontend:** server-rendered templates + light JS. Keep it simple early; the interactive map (late phase) is the one place that needs real client-side canvas work.
- **Rules data source:** D&D 5e **SRD** content, available under **CC-BY-4.0** (System Reference Document 5.1). Pull spell/ability/monster reference data from an open SRD source (e.g. Open5e / the 5e-SRD API), bundled locally where practical. **Attribute per CC-BY.** The SRD does not cover *every* official spell/monster (some are in paid books) — design so custom entries can be hand-added.
- **Privacy:** DM secrets and hidden entities must never leak to the player-facing API/templates. Treat visibility as a server-side filter, never a client-side hide.
- **AI (optional, later):** the workspace already has Claude API patterns (`stock-tracker`). Possible assist uses — generating an NPC, summarizing a session — are explicitly *late / optional*, not core.

## Phased plan

Ordering favors dependencies and the "local single-user first → auth → hosting → ambitious visuals last" arc the owner asked for. The visibility field is designed in from Phase 1; it is *enforced against players* in Phase 7.

- [x] **Phase 0 — Scaffold & shell.** Flask app (`app.py`, port 5002), SQLite bootstrap (`db.py`, `meta`/schema-version table), base layout with the tab navigation shell (Character / Spells & Actions / Map / Blog) + entity sidebar placeholder. No real data yet.
- **Tab split (Phase 7 cleanup):** the **Character Sheet** tab is now "your character" — a player lands directly on their own PC's sheet (DM sees the PC roster); **NPCs moved to their own tab** where the DM manages the cast and players see the NPCs they've **encountered** (revealed = `visibility='visible'`). `can_view_creature` lets players read-only view any non-hidden PC **or** NPC.

- [x] **Phase 1 — Creature engine + character sheets.** Six ability scores + modifiers (`floor((score-10)/2)`), HP, AC, resistances/immunities/vulnerabilities. Shared `creatures` table (migration 1) with `kind` ('pc'|'monster') and the `visibility` field ('visible'|'hidden'). `models/creature.py` owns the stat-block math + CRUD; Character Sheet tab does list/view/create/edit/delete. DM-only, local — player enforcement of `visibility` waits for Phase 7.
- [x] **Phase 2 — Dice roller.** `models/dice.py`: `parse_and_roll` is the single entry point — it dispatches an optional adv/dis suffix (`d20 adv`, `d20+3 dis`) to `roll_check`, else parses an expression (`2d6+3`, `1d20+1d4-1`, bounds-guarded). Dice tab uses a **confirm-before-roll** flow: quick buttons (d4–d100, adv/dis) only *populate* the expression box via JS (dice append, adv/dis replace); nothing rolls until the Roll button — keeps accidental clicks from flooding the roll log. Persisted log = migration 3 `rolls` table + `models/roll_log.py`; each entry shows a local-time stamp (relative time on hover), and a >5-min pause (`ROLL_GAP_SECONDS`) draws a divider grouping rolls into bursts (`_decorate_rolls`). Wired to the sheet: a per-ability **Check** button rolls `d20 + modifier`, logs it, flashes in place (`next` returns to the sheet; `_safe_next` blocks open redirects). Saving throws fold in once proficiency exists.
- [x] **Phase 3 — Spells/abilities + SRD rules reference.**
  - [x] **3a — Spell reference + Assist/Track mode.** SRD spells bundled locally (`data/spells.json`, CC-BY-4.0; `models/spells.py`). Spells & Actions tab: library with **client-side search + filters** (type: Cantrips/Spells; casting time: Action/Bonus/Reaction) → spell detail (stat block + description). (A non-spell "Actions" category arrives with the Phase 5 Actions/Abilities system.) A site-wide **Assist/Track** toggle (cookie, default `track`, in the top bar) governs behavior: in **assist** mode dice in rules text are clickable (`dicetext` filter → `.rollable` → base-layout JS posts to the dice engine) and a **Cast** button rolls the spell; in **track** mode dice are reference-only and casting is hidden. Mode is a personal UI pref — cookie is fine pre-auth (unlike roll colors, which need shared identity).
  - [x] **3a.1 — Glossary tooltips.** Reusable `info(key, display)` macro (`templates/macros.html`) renders an ⓘ with a hover/focus tooltip from `models/glossary.py` (`define` is a Jinja global so imported macros can reach it). Applied to spell components (V/S/M), school, saving throw, cantrip/level, and to ability codes + HP/AC on the character sheet. Drop `{{ info('term', 'Label') }}` next to any jargon, anywhere; unknown terms render plain.
  - [x] **3b — Character spellbooks.** `creature_spells` join (migration 4, slug + `prepared`, ON DELETE CASCADE) + `models/spellbook.py`. Character sheet shows known spells (prepared toggle, mode-aware rollable, remove); spells addable from both the sheet (dropdown of not-yet-known) and the spell page (dropdown of roster, known ones disabled). Shared `/spellbook/{add,remove,prepared}` routes keyed by form `creature_id`+`slug`+`next`.
- [x] **Phase 4 — Inventory, loot, currency, XP/leveling.** Migration 5 adds `xp`/`gold`/`silver`/`copper` to creatures; `models/creature.py` has the 5e XP table (`level_from_xp`, `xp_to_next`) — level stays manual (milestone-friendly) with XP-based level-up hints. Migration 6 + `models/inventory.py`: per-creature `creature_items` (name, quantity, description, equipped; ON DELETE CASCADE). Sheet shows a coin purse, XP progress, and an Inventory section with add / qty ± (clamped ≥1) / equip toggle / remove. **Equipment slots** (migration 7, `slot`+`hands`): fixed slot set (main/off hand, armor, helmet, cloak, gloves, boots, amulet, rings×2); `equip_item` enforces one-per-slot by swapping, and the two-handed rule (a 2H main-hand weapon blocks the off hand; equipping an off-hand item frees a 2H weapon). Sheet has a BG3-style slot panel (`equipped_by_slot`) with **drag-and-drop equip**: drag an inventory item onto its matching slot (HTML5 DnD; slot-match enforced client-side, equip/swap server-side via `/inventory/<id>/equip`). Equip buttons remain the keyboard-accessible fallback.
  - **4c — DM loot system.** Items are no longer created on the sheet (the free-form add box is gone; acquisition flows through loot). New **Loot tab** + `areas`/`loot_items` tables (migration 8), `data/items.json` premade catalog (`models/items.py`), and `models/loot.py`. Each **area** (named location, current one tracked in `meta.current_area_id`) has its own loot pool; the DM **spawns premade** or **creates custom** items into it, then **gives** an item to a roster member (copies into `creature_items`, leaves the pool). Loot persists per-area (no auto-clear; manual "Clear loot" + "Delete area"). **Loot is distributed to PCs only** (give dropdown lists PCs; server enforces `kind=='pc'`); **NPCs are stocked manually** by the DM via an add-item form that appears only on NPC sheets. Areas are a lightweight precursor to the fuller location system in Phases 9–10. *Note: true "players can't create, only DM" enforcement needs Phase 7 auth; for now it's structural.*
  - **Inventory UX:** equipment slot panel is a **BG3-style paper doll** (`SLOT_COLUMN`): armor in a left column, a character frame in the middle, weapons + jewelry on the right, sat **side by side with the inventory** (`gear-layout`) for short drag distances. The inventory is a **fixed-height scrollable panel** whose height is JS-synced to the equipment panel, with its own **name search + All/Equipped/Equippable filter** (state kept outside the `#gear` fragment so it survives AJAX re-renders). Renders **two distinct ring boxes** (Ring 1/Ring 2; model keeps one cap-2 `ring` slot, `equipment_panel` expands it). Each filled slot has an **unequip ×**. Gear actions (equip/unequip/qty/remove/drag/NPC-add) are **AJAX** — they re-render the `#gear` fragment (`_gear.html`) via fetch with no page reload/scroll-jump; handlers are delegated on `#gear` so they survive replacement; non-JS falls back to normal POST+redirect. Characters tab has a **name search + PC/NPC filter** (client-side) with color-coded kind badges.
- [x] **Phase 5 — Monsters, inspector, encounters.**
  - [x] **5a — Actions/Abilities system.** A **separate, creature-attached** action system, kept *distinct* from spells (decided): spells are SRD-backed with level/components; actions/abilities (Multiattack, Rage, breath weapons, legendary actions, passive traits) are free-form. Migration 9 `creature_actions` (name, `category` trait|action|bonus|reaction|legendary, optional `dice`, description; ON DELETE CASCADE) + `models/actions.py`. Sheet shows an **Actions & Abilities** section grouped by category, with mode-aware rollable dice (assist → `.rollable`, track → `die-text`) on both the `dice` field and dice in the description (`dicetext`). Works for PCs, NPCs, and monsters with no special-casing — so monster sheets get actions (and the existing spellbook) for free. **Actions are mainly *grabbed from a list*, not authored** (mirrors spells): an **action book** catalog (`data/actions.json`, `models/action_catalog.py`) is the primary add path on the sheet (a category-grouped dropdown) and is surfaced in the **Spells & Actions tab** behind a Spells/Action Book toggle with an add-to-roster control. Grabbing copies the entry onto the creature (like loot → inventory), so it can then be tweaked/removed per-creature; a secondary "custom action" form still allows one-offs. Shared `/actions/add` (copies from catalog when given a `slug`, else custom fields) + `/actions/<id>/remove`.
  - [x] **5b — Bestiary + read-only inspector.** Monsters reuse the creature engine wholesale: `kind='monster'`, the editable sheet is the same `character_detail` view, and create/edit/delete go through the shared character routes (`_form_to_data` now accepts `monster`; the edit form keeps the Type as Monster). New **Bestiary tab** (`list_monsters`, name search, create) and the **BG3-style read-only inspector** (`/bestiary/<id>/inspect`) — a trimmed creature view that previews the player's-eye view. Migration 10 adds `stats_revealed` (distinct from `visibility`): the inspector **masks the stat-block numbers** (AC/HP/abilities/defenses/actions/spells shown as `?`/silhouette) until the DM flips the reveal live from the inspector (`/bestiary/<id>/reveal`). Real enforcement against player logins is still Phase 7; for now it's a preview.
  - [x] **5c — Encounters.** Saved **named groups of monsters** to load into the combat tracker (Phase 6). Migration 11 `encounters` + `encounter_members` (creature_id + quantity, so 'Goblin ×4' is one row; ON DELETE CASCADE both ways) + `models/encounters.py`. Lives on the **Bestiary tab** (create + list with creature/type tallies) with an encounter detail page: add monsters (bestiary-only; adding a duplicate bumps quantity), quantity ± (drops the member at 0), remove, rename, delete. Loading into actual combat arrives with the Phase 6 tracker.
- [x] **Phase 6 — Combat tracker.** Migration 13 (`combats` + `combatants`) + `models/combat.py`. A **Combat tab** runs a live fight on the creature engine — PCs and monsters in one tracker. Combatants **snapshot their own HP/AC** when added (not the creature), so multiple instances of one stat block (Goblin 1..4) take damage independently and the fight never mutates the sheets. Build a fight by adding roster members or **loading a saved encounter** (also a "⚔️ Start combat" button on the encounter page). Features: per-combatant **initiative** (manual edit or `🎲 Roll initiative` = d20+DEX for all), display in **initiative order** with the active turn highlighted; **▶ Next turn** advances the turn pointer (`turn_combatant_id`, robust to add/remove) and bumps the **round** on wrap; **damage/heal** (damage absorbed by temp HP first, heal capped at max) with an HP bar that colours by % (green/amber/red); **temp HP** (set, not stacked); **conditions** (the 14 SRD conditions, toggled as chips); **short rest** (clears temp HP) / **long rest** (full HP, clears temp HP + conditions). Tracker actions are **AJAX** (re-render the `#combat` fragment in place; no-JS falls back to POST→redirect). **Combat history** (migration 14 `status`): "End combat" *archives* (kept in a History list to reopen or permanently delete) rather than deleting. **Initiative is one-time** (`initiative_rolled`): the prominent "Roll initiative" collapses to a de-emphasized re-roll after first use. **Rests moved to a new Party tab** (they belong to downtime, not a fight): the **Party tab** lists the PCs with HP and does party-wide **long rest** (all to full HP) / **short rest** (≈ half of each PC's missing HP, since hit dice aren't tracked) + a per-PC **damage/heal** amount control, operating on the real character sheets. (Combat itself no longer has rest buttons.) The Party **HP-adjust controls and the rests are DM-only**, gated by a server-side `is_dm()` helper (returns True pre-auth; both the route returns 403 and the UI is hidden for non-DMs) — see [[Phase 7]].
- [ ] **Phase 7 — Accounts, roles & the visibility spine (enforced).** Login, DM-vs-player permissions, server-side enforcement of public/private knowledge across every view. **This is the leap to a true multi-user web app.**
  - [x] **7a — Auth foundation.** Migration 15 `users` (role 'dm'|'player', `creature_id` PC link, `color` for 7d); `models/user.py` with **werkzeug-hashed** passwords. **Real passwords**, **shared signup code** model (decided with owner): first run with no users forces a **`/setup`** page to create the DM (admin); the DM sets a **signup code** (in `meta`) on the **Users tab**, and players self-register at **`/register`** with that code (role 'player'). `/login`, `/logout`, session-based; a `before_request` gate redirects anonymous visitors to login (public endpoints: login/logout/register/setup/static). `is_dm()` is now **real** (`current_user()` + role) — the DM-only chokepoint already guarding the Party HP-adjust + rests now enforces against actual player logins; `current_user`/`is_dm` injected into every template (topbar shows who's logged in + logout; the **Users** tab is DM-only). DM Users admin: set/clear signup code, assign each player to a PC, reset password, delete (can't delete self).
  - [x] **7b — Player ↔ character.** The DM **assigns and unassigns** players ↔ PCs on the Users tab (explicit Unassign + shows the linked PC name). A player with **no character** gets a "Create my character" prompt (`/character/create-mine`) that creates a PC (forced kind=pc, visible, player_name defaults to their username) and **auto-assigns** it to them; once they have one they can't create a second (it redirects to their sheet). Ownership scopes edits (7c).
  - [x] **7c — Visibility enforcement (core).** Server-side fog-of-war. `can_view_creature` / `can_edit_creature` + a `before_request` block of the whole DM-authoring surface (`_DM_ONLY_ENDPOINTS`: bestiary, encounters, combat, loot, party, users, create/delete character). A **player edits only their own PC** (edit/levelup/avatar/disposition/inventory/spellbook/actions routes guarded → 403 on others) and can **read-only inspect fellow party members** (`can_view_creature` = own PC or any non-hidden PC; NPCs/monsters still 404). The Character tab stays "mine" (own PC via `editable_roster`); the Party tab lists viewable party members, each clickable to a **read-only sheet** (all controls gated by a `can_edit` flag threaded through the sheet + `_gear`/`_spells`/`_actions` fragments; **Notes are private** to owner+DM; the DM's `visibility='hidden'` switch hides a PC from the party entirely). The spell library's "Viewing as" + add-to dropdowns are scoped to the viewer (player = own PC). DM sees/does everything. (Players seeing *discovered* NPCs/locations is deferred to the Phase 9 entity sidebar + blog, which the `visibility` flag will gate then; the **open question** about per-entry vs creature-level spell/item reveal is still open — currently creature-level.)
  - [x] **7d — Per-player roll colors.** Migration 16 adds `rolls.user_id` (ON DELETE SET NULL); `dice_roll` records the roller and `recent_rolls` joins the roller's name + `users.color`, so the shared dice log **tints each roll** (left border + roller name in colour). Each user sets **their own** colour on the Dice page (`/me/color`); the DM can set anyone's on the Users tab. Colours are validated server-side as hex (`#rgb`/`#rrggbb`) since they render into a `style` attr. Rolls stay server-authoritative.
  - [x] **7e — Player read-only Combat + Party.** The `party`, `combat`, `combat_detail` **views** are no longer in `_DM_ONLY_ENDPOINTS`, so players can open them; every **mutation** route stays DM-only (403). The combat tracker renders **read-only for players** — controls (`combat-controls`, add/load, per-combatant dmg/heal/temp/conditions-edit/remove, the help text) are wrapped in `{% if is_dm %}`; players see a static initiative number, a "watching — your DM runs the fight" tag, and read-only condition chips. The Party page shows the read-only overview (rest/HP controls already `is_dm`-gated; teammate cards aren't links — only the viewer's own PC is, tagged "you"). **Loot tab is now shared:** players see the current location's loot pool and can **Take** items to their PC (`loot_take`, current area only) and **Drop** items from their PC's inventory into it (`loot_drop` + `drop_to_loot`, own items only — 403 otherwise). The DM keeps all management (areas, spawn/create, give, remove); area/spawn/create/give/remove stay DM-only. **Currency loot:** a loot item can carry coins (migration 18 `gold`/`silver`/`copper` on `loot_items`; premade currency in `items.json`; a DM "Drop coins" form) — taking or giving it adds to the PC's **purse** instead of inventory (`is_currency` / `give_loot`). The character sheet's purse has a direct **± Adjust** (add/spend) control (`character_coins` → `adjust_coins`, clamped ≥ 0, owner/DM only).

**Magic items (migrations 19–20).** An equipped item can grant an **AC bonus**, **ability-score bonuses** (`stat_bonuses`, stored as `ability:amount` pairs), and/or **spells** (`grants_spells` slugs). These are applied by **computing** the creature's *effective* AC / ability scores / spell list from its equipped items (`effective_ac`, `effective_abilities`, `effective_spells`) — the base sheet is never mutated, so bonuses revert automatically on unequip/remove. The sheet shows effective values with a small `+N` tag and item-granted spells flagged "from <item>" (read-only); combat snapshots effective AC + DEX. **Body armor (migration 21)** follows 5e rules — `armor_type` (light/medium/heavy) + `armor_base` **set** the AC and cap how much DEX applies (light = full, medium = +2, heavy = none); `effective_ac` uses the worn armor's base + capped DEX; an **unarmored PC** is `10 + DEX` with optional **Unarmored Defense** (migration 22 `unarmored_defense`: barbarian +CON, monk +WIS); **NPCs/monsters keep their manual natural AC**; then additive `ac_bonus` (shield/ring/enhancement) stacks on top. (Armor with no base AC is ignored, never zeroes AC.) AC shows everywhere via the `eff_ac` filter with an `ac_breakdown` tooltip.

**Class system (the bones).** `data/classes.json` + `models/classes.py` define the 12 SRD classes (hit die, primary/save abilities, Unarmored Defense, spell ability, recommended stat array, starting equipment, gold). A creature records `class_name` (migration 23). Picking a class on the character form applies a **BG3-style starting kit** (`_apply_class` with `starting_kit`): recommended stats, **level-1 HP from the hit die + CON**, starting gold, and **auto-equipped starting gear** (which feeds the AC/armor system) — plus it sets Unarmored Defense (barbarian/monk). Player self-create always applies the kit; the DM form has an "Apply starting kit" checkbox. **Leveling ties in:** the level-up control pre-fills HP with the **fixed hit-die average + CON** (`hit_die_average`); the class shows on the sheet ("Level 5 Barbarian"). Deliberately *bones only* — per-level features, subclasses, and spellcasting slots are future work to layer on.

**Proficiency layer (migration 24).** The trained-bonus system on saves and skills (`models/proficiency.py`). `creature.proficiency_bonus(level)` = `2 + (level-1)//4` (+2 at 1–4 … +6 at 17–20). **Saving throws:** proficiency is *derived from the class* (each grants two, already in `classes.json`'s `saves`) — no per-creature storage; `save_table` returns each ability's save modifier (mod + prof when proficient). **Skill checks:** the 18 SRD skills (`SKILLS`, slug→ability) with per-creature proficiency stored in the `creature_skills` join (set on the character form's **DM-only** "Skill Proficiencies" picker via `set_skill_proficiencies`); `skill_table` returns each skill's modifier. **Skills are DM-controlled** — the picker is hidden from players and `set_skill_proficiencies` is gated by `is_dm()` in `character_edit` (players can still edit their own PC's other stats per 7c, but a crafted POST can't reset skills); a self-created player PC starts with none until the DM assigns them. Both are **computed on read** (never baked into the sheet), mirroring effective AC/abilities. The sheet's ability grid gains a **Save** button (beside Check) with a proficiency dot, and a new **Skills** section with per-skill Roll buttons — all posting `d20 + (modifier incl. prof)` through the existing dice engine (no engine change); the `signed` filter formats the computed modifier into the expression. Glossary terms added (proficiency, proficiency-bonus, skill, saving-throw-prof). *Still future:* class **skill-choice lists** (currently any skill is selectable, not constrained to the class's options), expertise/half-proficiency, and weapon/save proficiency for attack rolls.

DM item-creation forms (loot create + NPC gear) get a `magic_fields()` section (AC bonus, 6 stat bonuses, spell multi-select); premade magic items live in `items.json` (Shield +2 AC, Cloak/Ring of Protection +1, Amulet of Health CON+2, Belt of Giant Strength STR+2, Wand of Fire Bolt). Bonuses carry through loot give/take/drop. **Bestiary is now player-readable with a two-level monster fog of war** (`can_inspect_monster`): **level 1** = the DM toggles each monster's `visibility` (Show/Hide to players) — players' bestiary lists only *visible* monsters and 404s on hidden ones; **level 2** = the DM toggles `stats_revealed` (from the inspector) — a visible-but-unrevealed monster shows a masked card (🔒) + masked inspector (`?`), and reveal shows the stat block. Players get the **inspector only** (the full monster sheet stays DM-only); the DM gets both toggles (on the card and the inspector) + encounters/create.
  - **Open question (7c):** spellbook/ability/inventory visibility — *creature-level* (a hidden creature hides its whole stat block) vs optional *per-entry reveal*; if per-entry, add a `visibility` column to `creature_spells`/items then. Also lands **per-player roll colors**: rolls stay server-side/authoritative (anti-cheat, shared log); once players have identity, each gets a color and the dice log tints rolls by roller. (Deliberately deferred here rather than a pre-auth per-session hack.) **Open question to resolve here:** spellbook/ability/inventory visibility — default is *creature-level* (a hidden creature hides its whole stat block), vs optional *per-entry reveal* (fog-of-war on individual spells/items, like the DM-gated inspector). Decided to defer the choice until this phase; if per-entry, add a `visibility` column to `creature_spells`/items then.
- [ ] **Phase 8 — Hosting / deployment.** Put it online so friends can reach it (reuse the Cloudflare or AWS toolchains from the habit-tracker projects).
- [ ] **Phase 9 — World narrative layer.** Campaign blog + quest/objective log + the "entities you're aware of" sidebar, all wired to the shared entity model and visibility. (Track-only; could be pulled earlier if desired.)
- [ ] **Phase 10 — Maps.** Global + local maps with image backgrounds, position + interactable **overlay markers**, per-player visibility on markers. Most technically ambitious; intentionally last. (Creatures already carry an **`avatar`** — emoji or uploaded image, migration 12, rendered via the `avatar()` macro — meant to double as the map marker.)
- [ ] **Later / optional — Session scheduling.** Owner explicitly wants this near the end. Optional AI assists (NPC generation, session summaries) also slot here.
- [ ] **Phase 11 (idea, not committed) — Ambient/media embeds.** Owner floated embedding a **nested YouTube player** (e.g. background music / ambience, or a session VOD) in the site. Feasible via the YouTube IFrame embed/API; just an idea parked here, not scheduled. Notes: works as a standard `<iframe>` or the IFrame Player API; autoplay is blocked unless muted (browser policy); the page's CSP / `frame-src` must allow `youtube.com`/`youtube-nocookie.com`; prefer `youtube-nocookie.com` for privacy; consider a small persistent player (ambience that survives tab switches would need care since the app is server-rendered with full-page nav). Low effort if it's just an embed on the Blog/Map; higher if it must persist across navigation.
- [ ] **Phase 12 (idea, not committed) — Player-controlled companions & summons.** Owner wants **one PC per player to stay strict**, but to let players **control extra creatures**: DM-granted **NPC companions** and **summons** (familiars, conjured creatures) in future campaigns. Design: a companion/summon is just another *creature* (reuse the Phase-5 engine); add a **separate, looser "controls" relationship** distinct from the PC link — a `user_controls(user_id, creature_id)` table (or a `controlled_by` column), leaving `users.creature_id` (the single PC) untouched. Extend `can_view_creature`/`can_edit_creature` to "…or the player controls it" so control rides the existing visibility spine. **Summons** spawn from a catalog (like the action book / a monster pick), auto-grant control to the summoner, and are easy to dismiss (temporary). Surface a **Companions panel** on the Character tab; they act on the player's turn in the combat tracker (Phase 6 already treats PCs/monsters uniformly, so a controlled companion drops in). Not scheduled.

## Planned project layout (grows with phases)

```
dnd-campaign-tracker/
├── app.py              # Flask entry (Phase 0)
├── models/             # creature engine, entities, visibility
├── templates/          # tab views, sidebar, sheets
├── static/             # css/js; map canvas (Phase 10)
├── data/               # bundled reference data: SRD spells (CC-BY-4.0), item + action catalogs
├── campaign.db         # SQLite (gitignored)
├── requirements.txt
├── README.md
└── CLAUDE.md
```

## Running

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py          # serves at http://127.0.0.1:5002 (init_db runs on startup)
```

## Conventions

- **Two spines first.** When adding any feature, ask: does it reuse the creature engine? does it respect visibility? Don't fork either.
- **Visibility is server-side.** Never ship DM-hidden data to the client and hide it with CSS/JS.
- **SRD attribution.** Anything sourced from the 5e SRD must carry CC-BY-4.0 attribution.
- **DM data vs player data** stay clearly separated in queries; default a query to the *least* privileged view and widen for the DM, not the reverse.
- **No build step** unless a phase truly needs one (likely only the map).
- **Commits / PRs: no AI attribution.** Do **not** add a `Co-Authored-By: Claude …` trailer, a "Generated with Claude Code" line, or otherwise list Claude/AI as a contributor on commits or pull requests. Keep commit messages plain.

## D&D glossary (for the owner — and future sessions)

- **DM (Dungeon Master)** — runs the game and the world; the admin role here.
- **PC** — player character. **NPC** — non-player character (run by the DM).
- **Ability scores** — STR, DEX, CON, INT, WIS, CHA. Most numbers derive from these via a **modifier**.
- **Check / saving throw** — `d20 + modifier` vs a **DC** (Difficulty Class). Check = attempt something; save = resist something.
- **AC (Armor Class)** — how hard a creature is to hit. **HP (Hit Points)** — health.
- **Initiative** — turn order in combat (`d20 + DEX modifier`).
- **Condition** — status effect (poisoned, prone, stunned, frightened, unconscious, …).
- **Spell slot** — a consumable resource for casting; refreshes on a **long rest**.
- **Short rest / long rest** — recovery mechanics that restore HP / resources.
- **Encounter** — a prepared set of monsters for a fight.
- **Stat block** — the bundle of a creature's combat-relevant numbers.
- **SRD** — System Reference Document; the openly-licensed (CC-BY-4.0) subset of D&D 5e rules we draw reference text from.
- **dNN notation** — `d20` = roll a 20-sided die; `2d6+3` = roll two d6 and add 3.
